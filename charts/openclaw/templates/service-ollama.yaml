apiVersion: v1
kind: Service
metadata:
  name: openclaw-ollama
  labels:
    app: openclaw-ollama
spec:
  type: ClusterIP
  selector:
    app: openclaw-ollama
  ports:
    - name: http
      port: 11434
      targetPort: 11434
      protocol: TCP
