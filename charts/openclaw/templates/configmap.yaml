apiVersion: v1
kind: ConfigMap
metadata:
  name: openclaw-config
  labels:
    app: openclaw
data:
  openclaw.json: |-
    {
      "gateway": {
        "mode": "local",
        "bind": "lan",
        "port": 18789,
        "trustedProxies": ["172.17.0.1", "10.42.0.0/16"],
        "auth": {
          "mode": "trusted-proxy",
          "trustedProxy": {
            "userHeader": "X-Auth-Request-Email",
            "requiredHeaders": ["X-Auth-Request-Access-Token"]
          }
        },
        "controlUi": {
          "enabled": true,
          "dangerouslyDisableDeviceAuth": true,
          "allowedOrigins": [
            "http://rtx2000-pro-bw-se.teknoir:31789",
            "https://openclaw-rtx2000-pro-bw-se-teknoir-demo.teknoir.cloud",
            "https://openclaw-rtx2000-ada-128gb-se-teknoir-demo.teknoir.cloud",
            "https://openclaw.teknoir.cloud"
          ]
        }
      },
      "models": {
        "providers": {
          "vllm": {
            "apiKey": "vllm-local",
            "baseUrl": "http://vllm:8080/v1",
            "api": "openai-completions",
            "timeoutSeconds": {{ .Values.models.providerTimeoutSeconds }},
            "models": [
              {
                "id": "{{ .Values.models.id }}",
                "name": "{{ .Values.models.id }}",
                "reasoning": true,
                "input": [
                  "text"
                ],
                "cost": {
                  "input": 0,
                  "output": 0,
                  "cacheRead": 0,
                  "cacheWrite": 0
                },
                "contextWindow": 131072,
                "maxTokens": 131072
              }
            ]
          }
        }
      },
      "agents": {
        "defaults": {
          "workspace": "~/.openclaw/workspace",
          "timeoutSeconds": {{ .Values.models.agentTimeoutSeconds }},
          "model": {
            "primary": "{{ .Values.models.primary }}"
          },
          "pdfModel": {
            "primary": "{{ .Values.models.primary }}"
          },
          "imageModel": {
            "primary": "{{ .Values.models.primary }}"
          }
        },
        "list": [
          {
            "id": "default",
            "name": "OpenClaw Assistant",
            "workspace": "~/.openclaw/workspace"
          }
        ]
      },
      "plugins": {
        "allow": ["openclaw-mcp-bridge"],
        "entries": {
          "openclaw-mcp-bridge": {
            "config": {
              "mode": "direct",
              "servers": {
                "mcp-swedish-law": {
                  "transport": "streamable-http",
                  "url": "http://mcp-swedish-law/mcp",
                  "description": "Swedish statutes and regulations"
                }
              },
              "toolPrefix": true
            }
          }
        }
      },
      "cron": { "enabled": true },
      "diagnostics": {
        "enabled": true,
        "stuckSessionWarnMs": {{ int .Values.models.stuckSessionWarnMs }},
        "stuckSessionAbortMs": {{ int .Values.models.stuckSessionAbortMs }}
      },
      "session": {
        "writeLock": {
          "maxHoldMs": {{ int .Values.models.sessionWriteLockMaxHoldMs }}
        }
      }
    }
  AGENTS.md: |-
    # OpenClaw Assistant

    You are a helpful AI assistant running in Kubernetes.
