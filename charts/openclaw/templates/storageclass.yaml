apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: openclaw-local-path
  labels:
    app: openclaw
provisioner: rancher.io/local-path
reclaimPolicy: Delete
volumeBindingMode: WaitForFirstConsumer
parameters:
  path: /opt/teknoir/openclaw
