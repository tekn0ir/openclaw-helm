apiVersion: v1
kind: Service
metadata:
  name: mcp-swedish-law
  labels:
    app: mcp-swedish-law
spec:
  type: ClusterIP
  selector:
    app: mcp-swedish-law
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP
