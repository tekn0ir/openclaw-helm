# Image configuration
image:
  repository: tekn0ir/openclaw
  tag: latest
  pullPolicy: Always

# MCP Servers configuration
mcp:
  swedishLaw:
    image: tekn0ir/swedish-law-mcp
    tag: latest
    pullPolicy: Always
