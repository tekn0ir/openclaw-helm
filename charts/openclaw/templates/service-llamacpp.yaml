apiVersion: v1
kind: Service
metadata:
  name: openclaw-llamacpp
  labels:
    app: openclaw-llamacpp
spec:
  type: ClusterIP
  selector:
    app: openclaw-llamacpp
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
