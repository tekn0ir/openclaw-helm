apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: openclaw-ollama-pvc
  labels:
    app: openclaw-ollama
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
  storageClassName: openclaw-local-path
